name: Flutter CI

on:
  pull_request

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Install Flutter SDK
        run: |
          git clone https://github.com/flutter/flutter.git -b stable --depth 1
          echo "${GITHUB_WORKSPACE}/flutter/bin" >> $GITHUB_PATH

      - name: Set up Dart SDK (obrigatório para comandos Dart e Flutter)
        uses: dart-lang/setup-dart@v2
        with:
          sdk-version: stable

      - name: Install dependencies
        run: flutter pub get

      - name: Static analysis
        run: flutter analyze

      - name: Run tests
        run: flutter test

      # Opcional: checar formatação do código
      # - name: Verify formatting
      #   run: dart format --output=none --set-exit-if-changed .

      # Aqui você pode adicionar etapas de build de aplicativos, geração de APK, upload para Firebase, etc.
      # Exemplo para gerar APK do Flutter:
      # - name: Build APK
      #   run: flutter build apk --release

      # Se desejar fazer deploy, considere adicionar passos para:
      # - Publicar no Google Play (usando fastlane ou a ação do Google)
      # - Publicar no Firebase App Distribution
      # - Criar artefatos de build para download
